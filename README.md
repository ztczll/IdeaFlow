# IdeaFlow

为个人产品经理打造的一款轻量级 Web 应用，专注于"捕获-整理-验证"闭环，帮助他们持续发现、结构化并验证潜在 AI 需求。

## 功能特性

- 🚀 **快速捕获** - 一键记录需求想法，防止好点子被遗忘
- 📊 **需求管理** - 完整的需求生命周期管理，从收集到归档
- 📈 **数据可视化** - 仪表盘展示需求管道、趋势和统计
- 👥 **团队协作** - 支持单用户和团队用户，数据共享
- 🎨 **现代UI** - 基于设计文档的精确样式实现

## 技术栈

- **框架**: Next.js 14+ (App Router)
- **语言**: TypeScript
- **样式**: Tailwind CSS
- **数据库**: Vercel Postgres + Prisma ORM
- **认证**: NextAuth.js
- **数据获取**: React Query (TanStack Query)
- **UI组件**: shadcn/ui
- **图表**: Recharts

## 快速开始

### 环境要求

- Node.js 18.17.0 或更高版本
- npm 或 yarn

### 安装依赖

```bash
npm install
```

### 环境变量

创建 `.env` 文件：

```env
DATABASE_URL="your-postgres-connection-string"
NEXTAUTH_SECRET="your-secret-key"
NEXTAUTH_URL="http://localhost:3000"
```

### 数据库设置

```bash
# 生成 Prisma 客户端
npx prisma generate

# 运行数据库迁移
npx prisma migrate dev

# 初始化种子数据
npm run db:seed
```

### 开发运行

```bash
npm run dev
```

访问 [http://localhost:3000](http://localhost:3000)

### 构建部署

```bash
npm run build
npm start
```

## 项目结构

```
IdeaFlow/
├── app/                    # Next.js App Router
│   ├── (auth)/            # 认证页面
│   ├── (main)/            # 主应用页面
│   └── api/               # API 路由
├── components/            # React 组件
│   ├── ui/               # 基础 UI 组件
│   ├── layout/           # 布局组件
│   ├── requirements/     # 需求相关组件
│   └── dashboard/        # 仪表盘组件
├── lib/                  # 工具函数和配置
├── prisma/               # Prisma 配置
├── types/                # TypeScript 类型定义
└── data/                 # 种子数据
```

## 部署到 Vercel

1. 将代码推送到 GitHub
2. 在 Vercel 中导入项目
3. 配置环境变量
4. 连接 Vercel Postgres 数据库
5. 部署

## 许可证

MIT License
